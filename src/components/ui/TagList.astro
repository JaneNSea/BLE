---
interface Props {
  tags: string[];
}

const { tags } = Astro.props;
---

{tags.length > 0 && (
  <ul class="tags" aria-label="技术标签">
    {tags.map((tag) => <li class="tag">{tag}</li>)}
  </ul>
)}
